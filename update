#!/run/current-system/sw/bin/env bash

ENV=/run/current-system/sw/bin/env
${ENV} git fetch -a
${ENV} git checkout -f origin/master
${ENV} nix flake update --commit-lock-file
${ENV} git push origin HEAD:master
